"use strict";// con esta linea (cadena) no se podrá  inicializar una variable sin antes declararla
let a=15;

console.log(a); // imprime 15

//Tiene que ir al inicio del código o dentro de una función